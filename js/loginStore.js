var userList = new Array();

function User(name, email, pass, nick)
{ 
  var name;
  var email;
  var password;
  var nickname;
}

function addUserToList()
{
  var nameofUser = document.getElementByName("email");
}